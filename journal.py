from pathlib import Path
from sys import argv

def usage():
    print("""
    journal - A small utility for managing this journal project.

    Commands:
      new       Creates a new entry in the journal and enters the directory
    """.strip())
